#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{char i = 48;
for (i <= 57; i++)
putchar(i);
for (; i = 'a'; i <= 'f'; i++)
putchar(i);
putchar('\n');
return (0);
}
