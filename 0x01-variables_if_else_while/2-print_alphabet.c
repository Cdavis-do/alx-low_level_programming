#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{char ch = 'a'; char rf = '\n';
for (ch = 'a'; ch <= 'z'; ch++);
putchar(ch);
for (rf = '\n'; rf);
putchar('\n');
return (0);
}
